---
title: "2.4 参数初始化"
source_docx: "第1部分 深度学习/2.神经网络训练的常用方法/2.4 参数初始化.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 2.4 参数初始化

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、Xavier（Glorot）初始化

为了避免梯度消失或爆炸，我们希望每一层输出的方差与输入的方差大致相等。除了在每次计算结束后用批量规范化“拉回来”之外，我们还希望计算过程本身就能保证这一点。在线性网络中，如y_j=sigma(x_i*w_ij)+b_j，输入与输出方差的关系取决于参数的取值，因此参数如何初始化至关重要。

假设权重和输入独立同分布，且它们的均值都为 0。权重的方差为sigma^2，这是我们要解的。输入的方差为Var(x_i)=Var(X)。

考虑一个简单的线性层，输出y_j=sigma(x_i*w_ij)（忽略偏置），对任意j，Var(y_j)=sigma(Var(x_i*w_ij))=n_in*Var(x_i*w_ij)（n_in为输入数量）

> [图片内容待重建：img-1c8c1a7307bf-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
前向传播的目标是保持方差不变，即 Var(Y) = Var(X)，故Var(W)=1/n_in

同理，对于反向传播，为了保持梯度不变性，我们希望：

> [图片内容待重建：img-1c8c1a7307bf-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里z_l是第l层的输出（尚未经过激活函数）。接下来进行推导：

> [图片内容待重建：img-1c8c1a7307bf-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
反向传播中，对于前一层（第l-1层）的一个神经元j，L对它的梯度是第l层的全部（第1至n_out个）神经元传播而来的梯度的总和。故需要Var(W)=Var(w_k,j)=1/n_out.

实际操作中，取二者的调和平均数2/(n_in+n_out)作为方差。

## 二、Kaiming初始化

若激活函数使用ReLU，f'(z)在z>0时为 1，在z<0时为 0，其方差：

> [图片内容待重建：img-1c8c1a7307bf-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-1c8c1a7307bf-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
得到W的方差应为2/n_in.

反向传播：

设sum是L对第l层输入a的梯度（包含了第l层n_out个神经元传来的梯度之和），f’(z)是激活函数的梯度，则由独立随机变量乘积方差公式：

> [图片内容待重建：img-1c8c1a7307bf-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
则W的方差应为2/n_out.

一般来说，取前向传播对应的方差2/n_in即可达到足够好的效果（一般用2/n_in和2/n_out初始化差别也不大）。

## 参考文献

暂无已核验参考文献。
