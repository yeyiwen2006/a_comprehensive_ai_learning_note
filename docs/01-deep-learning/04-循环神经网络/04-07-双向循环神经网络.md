---
title: "4.7 双向循环神经网络"
source_docx: "第1部分 深度学习/4.循环神经网络/4.7 双向循环神经网络.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 4.7 双向循环神经网络

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、隐马尔可夫模型中的动态规划

> [图片内容待重建：img-6af827492895-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6af827492895-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
1.前向递归（“对上次递推”）

> [图片内容待重建：img-6af827492895-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
上式中，pi_t(h_t)表示的是所有“得到(h_1,...,h_t-1,h_t,x_1,..,x_t-1)的组合的概率”求和（这里h_1,...,h_t-1有多种组合要求和，x_1,..,x_t-1是给定的），也就是输出x_1,..,x_t-1并得到t时刻的隐状态h_t的总概率，是“实际发生概率”.从而等号右边表示：在各种h_T选择下，得到(x1,...,xT)的概率，对h_T求和（pi_t(h_t)内部已经隐含了对h_1,...,h_t-1的各种组合的求和，也隐含了得到x_1,..,x_T-1的概率，故这里只需要对h_T求和）。

> [图片内容待重建：img-6af827492895-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
2.后向递归（“对下次递推”）

这里我们设的ρ_t(h_t)是“在已经得到h_t，x_t条件下，最终得到(x_t+1,...,x_T)的概率”，是“给定状态下的预测概率”，包含了对经过的各种可能的h_t+1,...,h_T路径概率的求和.

> [图片内容待重建：img-6af827492895-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6af827492895-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
注：这里ρ1(h1)内部已经包含了对经过各种h2,...,hT路径的求和.

> [图片内容待重建：img-6af827492895-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6af827492895-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
为保证t=T时公式形式的一致性，初始化ρT(hT)=1.

3.二者的结合

如果我们只知道时间序列输出x_1,...,x_T中除了x_j之外其他所有元素，但不知道隐状态h_1,...,h_T，这时如果逐个枚举每个h_j的全部可取的值（假设每个h_j的取值空间有k个值），其计算复杂度为O(k^T)，显然行不通.

这里我们采用前向递归与后向递归结合的方法：

> [图片内容待重建：img-6af827492895-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
（这里分母是得到除了x_j之外其他所有元素的概率，是和隐状态无关的常数）

> [图片内容待重建：img-6af827492895-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
从计算复杂度的角度，无论是前向递归还是后向递归，对T个隐变量的情况对(h_1,...,h_T)的所有取值情况求和，递归计算过程的计算复杂度均为O(kT).上述公式相当于x_j以前取前向递归，以后取后向递归，计算复杂度仍然为O(kT).（注：这里的计算复杂度是执行计算时的，并不包括训练阶段训练概率值之类的）

## 二、双向循环神经网络

在序列学习中，我们以往假设的目标是：在给定观测的情况下（例如，在时间序列的上下文中或在语言模型的上下文中）， 对下一个输出进行建模。但实际上，对于文章挖空填空这样的任务，每个短语的“下文”传达了重要信息（如果有的话），而这些信息关乎到选择哪个词来填空， 所以无法利用这一点的序列模型将在相关任务上表现不佳。例如，如果要做好命名实体识别 （例如，识别“Green”指的是“格林先生”还是绿色）， 不同长度的上下文范围重要性是相同的。

> [图片内容待重建：img-6af827492895-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6af827492895-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6af827492895-0013] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
