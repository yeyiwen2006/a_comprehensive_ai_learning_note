---
title: "14.2 基于模型的强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.2 基于模型的强化学习.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.2 基于模型的强化学习

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、模型预测控制（Model Predictive Control, MPC）

以下棋为例。在无模型（Model-Free）的算法中，我们看到当前棋局，直接凭经验落子（策略网络直接输出动作）。而MPC则是：看到当前棋局，我在脑海里（环境模型）推演：如果我下A处，对手可能下B，那我再下C……推演H步之后，发现局势大好，于是我决定现在的第一步就走A。

> [图片内容待重建：img-bb333f0e2ab5-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
由于动作空间可能是连续且高维的，直接用数学公式解出上面的argmax很难。为此，我们可以采取如下操作：

### （一）打靶法

简单粗暴，随机生成1000条动作轨迹，扔进模型里跑一下，看哪条轨迹得分最高，就选哪条。在高维空间中，随机乱猜很难猜中最优解，效率极低。

### （二）交叉熵方法（Cross-Entropy Method, CEM）

1.核心步骤

（1）初始化：假设动作服从某个分布（比如正态分布）。

（2）采样：从这个分布里采样一批动作序列。

（3）评估：用模型评估这些序列的得分。

（4）筛选精英：挑出得分最高的那些序列（比如前10%）。

（5）进化：用这些精英序列的统计特征（均值和方差）来更新分布的参数。让分布的中心向高分区域移动，并缩小方差（更确信）。

（6）迭代：重复上述过程，直到分布收敛。

2.和交叉熵的关系

CEM是一种进化算法，它从当前分布中生成一堆样本，挑出表现最好的精英样本，它认为：“这前10%的精英样本就是真理，就是正确答案。”于是它倾向于更新分布参数（比如高斯分布的u和sigma），使得这个分布生成这些“精英样本”的概率最大，即我们让当前的分布去拟合精英样本的分布。衡量这两个分布差异的指标就是交叉熵，推导如下：

> [图片内容待重建：img-bb333f0e2ab5-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 二、PETS算法（Probabilistic Ensembles with Trajectory Sampling）（UC Berkeley，NeurIPS 2018）

模型是不完美的。如果环境模型预测错了，MPC就会根据错误的信息规划出错误的策略。PETS引入了概率（Probabilistic）和集成（Ensembles）来量化这种风险。

### （一）两种不确定性

> [图片内容待重建：img-bb333f0e2ab5-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （二）PETS的策略

> [图片内容待重建：img-bb333f0e2ab5-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
后续第二、三阶段为利用模型的输出进行前向计算。若干次前向计算后，我们会用收集到的数据训练环境模型，损失函数见后面（三）部分所示。

> [图片内容待重建：img-bb333f0e2ab5-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0013] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0014] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0015] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
第二、三阶段是前向计算，也就是“走一步”。走一定步数之后，模型会用这些步数采集到的数据训练环境模型。

> [图片内容待重建：img-bb333f0e2ab5-0016] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0017] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
也就是说，对那些总体得分较高且各环境模型之间没有较大分歧的动作，模型才会给它们较高的A_i值，使其被选进前10％“精英”中，被采用进真实动作分布。这种策略对一些容错率低的环境尤其有用。

### （三）环境模型（M1-M5）的损失函数

本质上，这是在强制用高斯分布拟合的前提下，取预测正确概率的最大似然。

> [图片内容待重建：img-bb333f0e2ab5-0018] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0019] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0020] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-bb333f0e2ab5-0021] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 三、MBPO算法（Model-Based Policy Optimization）

MBPO结合了Model-Based的高数据利用效率和Model-Free（如SAC）的高性能。

### （一）基于模型方法的复合误差问题

纯粹的 Model-Based 方法（如PETS）完全依赖模型推演。如果模型第一步预测偏了 1%，第二步就是基于偏了的数据再预测，误差会指数级累积。推演越长，结果越不可信。

### （二）MBPO的创新：分支推演

传统思路是从s_0开始一直用模型推演到底，但MBPO并不如此。它的步骤如下：

> [图片内容待重建：img-bb333f0e2ab5-0022] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
每次推演都从“真实状态”出发，好比每走几步就看一眼地图（真实数据），消除了累积误差。虽然只推演几步，但可以生成成千上万条这样的短数据。对于SAC这样的算法来说，数据量越大，训练越稳。MBPO证明了，只要模型推演的步数k控制得当（平衡了模型误差和数据增益），基于模型的方法可以比无模型方法快很多倍（采样效率高），且最终效果一样好。

### （三）MBPO的工作流

初始化与预热

> [图片内容待重建：img-bb333f0e2ab5-0023] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
大循环：

总的来说，MBPO继承了PETS的集成环境模型（M1-M5）以及其更新过程（即第二阶段）。但MBTO没有用PETS直接用环境模型决定策略的方式，而是像Dyna-Q那样，取一个过去出现过的状态，根据当前策略预测起到为主函数提供数据的辅助作用（即第三阶段），主函数利用真实数据和环境数据进行更新（即第四阶段）。

第一阶段：真实交互

> [图片内容待重建：img-bb333f0e2ab5-0024] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
第二阶段：环境模型更新（把最新现实输入给环境模型，类似PETS，对集成环境模型M1-M5进行更新，损失函数也和PETS完全相同）

> [图片内容待重建：img-bb333f0e2ab5-0025] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
第三阶段：分支推演（让环境模型输出推演数据，喂给SAC）

> [图片内容待重建：img-bb333f0e2ab5-0026] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这一阶段与Dyna-Q不同的是，Dyna-Q直接取了(s_t,a_t)，仅仅预测s_t+1,r_t。而这里只取一个s_real，后面都根据当前策略推演。

第四阶段：策略优化（利用真实数据和环境模型数据，训练SAC）

> [图片内容待重建：img-bb333f0e2ab5-0027] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里的第四阶段类似于把Dyna-Q的“利用上次真实交互更新”和“利用环境更新”合为一体，更好地并行计算。

伪代码：

初始化;

先随机玩 5000 步存入真实池;

for epoch in range(total_epochs):

--- 步骤 A: 真实交互 ---

根据SAC的策略执行一步交互;

存入模型经验池;

--- 步骤 B: 训练/更新模型 ---

if epoch % 250 == 0: # 每 250 步真实交互，重新训练一次模型

使用真实数据训练模型，以最小化Negative Log-Likelihood;

--- 步骤 C: 分支推演 (数据生成) ---

从真实池中选取起点;

for i in range(k_steps): # k步推演

随机选一个模型网络，由当前策略预测下一个状态和奖励;

存入模型经验池;

状态更新，准备下一步推演;

--- 步骤 D: 策略更新 (SAC) ---

for _ in range(G_updates): # 环境每走1步，SAC训练G次(如 20 次)

混合采样(模型数据占比通常很高);

更新SAC参数;

## 参考文献

暂无已核验参考文献。
