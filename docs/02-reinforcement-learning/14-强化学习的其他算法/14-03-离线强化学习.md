---
title: "14.3 离线强化学习"
source_docx: "第2部分 强化学习/14.强化学习的其他算法/14.3 离线强化学习.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 14.3 离线强化学习

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、离线强化学习的定义和主要方法

离线强化学习的核心在于：智能体不与环境交互，仅从已收集的静态数据集（Experience Replay Buffer）中学习策略。这解决了现实场景（如自动驾驶、医疗、推荐系统）中在线试错成本过高或反馈滞后的问题。

在在线策略（On-policy）或离线策略（Off-policy）算法中，智能体可以通过交互修正误差。但在纯离线设定下，直接使用传统Off-policy算法（如DDPG）会失败，因为数据集中状态-动作对(s, a)的分布与当前策略pi产生的分布不匹配，价值函数Q(s, a)会对数据集中未出现的动作（Out-of-Distribution, OOD）给出错误的高估值。由于智能体无法去环境验证，这个错误会不断累积，导致策略失效。

为了解决这个问题，主要有两种思路：限制策略选择（如BCQ）和 限制价值函数（如CQL）。

## 二、批量限制Q-learning（BCQ）

BCQ的核心思想是：为了解决“外推误差” (Extrapolation Error)，我们将策略限制在离线数据集（Batch）的分布范围内，并在该范围内寻找最优动作。

### （一）模型组成

> [图片内容待重建：img-24245997a3db-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （二）工作流

1.训练VAE

> [图片内容待重建：img-24245997a3db-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
具体训练方式见后文。

2.计算目标Q值

在Q-learning中，目标Q值为max(奖励+下一状态s’的Q值)。这里的难题是怎么估计下一状态s’的Q值。

> [图片内容待重建：img-24245997a3db-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
3.更新Q网络

> [图片内容待重建：img-24245997a3db-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
4.更新扰动网络（见后文）

### （三）VAE的训练

> [图片内容待重建：img-24245997a3db-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （四）为什么要用VAE，而不能用回归、交叉熵等方式？

1.回归的问题

> [图片内容待重建：img-24245997a3db-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
2.交叉熵的问题

> [图片内容待重建：img-24245997a3db-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
3.VAE的好处

把VAE的编码器和解码器合起来，实际上就是一个“模仿器”，其能有效在给定a的情况下，“模仿”出一批相似的a，无论a的分布如何。

> [图片内容待重建：img-24245997a3db-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （四）扰动网络的更新

> [图片内容待重建：img-24245997a3db-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0013] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 三、保守Q-learning（CQL）

CQL算法基于SAC算法，对Q函数的更新方式进行了修改。既然在无法与环境互动的情况下，未知动作容易被高估，那就直接在Q函数的更新目标中加入惩罚项，人为压低没见过的动作的Q值。

> [图片内容待重建：img-24245997a3db-0014] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0015] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-24245997a3db-0016] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
- [待补引用] 未自动检索到明确参考文献线索，建议人工补充可追溯来源。
