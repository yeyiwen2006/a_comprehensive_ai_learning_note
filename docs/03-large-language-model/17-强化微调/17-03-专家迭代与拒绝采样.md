---
title: "17.3 专家迭代与拒绝采样"
source_docx: "第3部分 大语言模型/17.强化微调/17.3 专家迭代与拒绝采样.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 17.3 专家迭代与拒绝采样

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、专家迭代（Expert Iteration）的基本思想

> [图片内容待重建：img-0b708e73140c-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-0b708e73140c-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-0b708e73140c-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
更新参数的式子看起来是监督学习，实际上却完成了“策略提升”的过程，是给定环境和奖励下的强化学习。当然，前面讲蒙特卡洛树搜索时也讲到过专家迭代思想下不用监督微调的更新方式的方法。

> [图片内容待重建：img-0b708e73140c-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 二、拒绝采样微调（Rejection Sampling Fine-Tuning，RFT）

> [图片内容待重建：img-0b708e73140c-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 三、STaR（Self-Taught Reasoner）：从错误后修正的推理中学习

模型生成推理过程和答案。如果答案错误，向模型注入正确答案（Hint），要求模型生成能够推导出正确答案的推理过程（Rationalization）。将这些“后视修正”的推理数据加入训练集，进行监督微调。

## 四、专家迭代思想的应用

1.LLM

一般如果后续还要用PPO进行针对推理的优化，通常会先跑几轮专家迭代。PPO对初始策略（Initial Policy）很敏感，我们需要一种方式把模型的分布拉到一个较高的水平（SFT后的模型往往还是不够强），再上PPO去优化那些极其困难的Case或进行对齐。专家迭代就适合作为这种方式。而对于GRPO，它是基于组内相对优势更新的，这几个对的样本会产生极大的正向梯度，每一轮更新，模型都在对自己生成的“好样本”进行强化，对“坏样本”进行抑制，实质上起到了专家迭代的作用，免去了“评估搜索结果”这个消耗额外算力的环节。

2.AlphaZero

通过原始蒙特卡洛树搜索（而不是针对LLM的改进版本），探索可能的路径。通过交叉熵损失，让策略网络的输出接近蒙特卡洛树搜索方法采样的结果；价值网络拟合的目标价值则直接是最终的输赢结果（只有最终奖励1或-1），即希望输出的预测值v能准确预测最终结果z。

> [图片内容待重建：img-0b708e73140c-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-0b708e73140c-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。

## 参考文献

暂无已核验参考文献。
