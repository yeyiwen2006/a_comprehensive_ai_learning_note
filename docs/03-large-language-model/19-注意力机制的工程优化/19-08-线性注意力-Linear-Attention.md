---
title: "19.8 线性注意力（Linear Attention）"
source_docx: "第3部分 大语言模型/19.注意力机制的工程优化/19.8 线性注意力（Linear Attention）.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 19.8 线性注意力（Linear Attention）

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、基本原理

> [图片内容待重建：img-a411e6016a52-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
实现线性化并非无条件的，必须去掉Softmax，这是因为：

> [图片内容待重建：img-a411e6016a52-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
我们需要一个核函数来近似Softmax函数。具体数学推导：

> [图片内容待重建：img-a411e6016a52-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-a411e6016a52-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
注：内积表示行向量*列向量=标量；外积表示列向量（m*1）*行向量（1*n）=矩阵（m*n）。

> [图片内容待重建：img-a411e6016a52-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-a411e6016a52-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
*注：K和V本来分别为n*d_k和n*d_v维，K转置后（函数fai不影响元素个数）和V矩阵相乘，容易知道矩阵相乘计算复杂度为n*d_k*d_v，也就是O(n)。

## 二、如何寻找核函数fai(x)？

对于 sim(q, k) = exp(q · k^T)=fai(q) · fai(k^T)

在当我们在深度学习模型（如Transformer的原始注意力机制）中直接使用Softmax时，我们是在进行数值计算，对于单个数值x，exp(x)是一个成熟的数学函数，计算机可以高效处理。但当q和k是两个向量时：

> [图片内容待重建：img-a411e6016a52-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-a411e6016a52-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-a411e6016a52-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-a411e6016a52-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
计算 exp(q·k) 这个简单的操作，在数学上等价于：将向量 q 和 k 映射到一个无限维的特征空间，在这个无限维空间中，计算它们的内积，这个无限维空间包含了原始向量的所有可能的多项式组合（一次项、二次项、三次项...）。

线性注意力的出现本身就是为了解决长序列下O(n^2)的问题，但如果用这个函数，它自身将需要构造一个趋于无限维的特征空间，在一个本身就趋于无限长的序列上，这是计算机无法处理的。

因此我们必须对这个函数进行近似。常见近似方法：

> [图片内容待重建：img-a411e6016a52-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
但这些核函数都不能足够好地拟合Softmax，这是线性注意力性能不如Full Attention的核心原因。

## 三、在自回归中的优势

线性化注意力还有一个独特优势：它可以被轻松地改写为递归形式，非常适合自回归推理。

> [图片内容待重建：img-a411e6016a52-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这就像一个RNN，在生成第 t 个token时，我们已经有了前 t-1 个token的完整总结 S_{t-1} 和 Z_{t-1}。当新的第 t 个token产生时，我们不需要回溯整个历史，只需要将当前token的贡献 ϕ(k_t)^T v_t 加到 S_{t-1} 上，将 ϕ(k_t)^T 加到 Z_{t-1} 上，就得到了新的状态 S_t 和 Z_t，然后用当前查询 q_t 与最新的状态交互：o_t = (ϕ(q_t) S_t) / (ϕ(q_t) Z_t)，得到当前时刻的输出。

因此，我们不需要存储整个键值的历史，只需要维护一个固定大小的状态 S 和 Z。每一步的计算成本是常数 O(1)（相对于序列长度），极大地提高了长文本生成的效率。

## 参考文献

暂无已核验参考文献。
