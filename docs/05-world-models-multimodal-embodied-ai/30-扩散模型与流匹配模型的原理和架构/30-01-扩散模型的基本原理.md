---
title: "30.1 扩散模型的基本原理"
source_docx: "第5部分 世界模型、多模态生成与具身智能/30.扩散模型与流匹配模型的原理和架构/30.1 扩散模型的基本原理.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 30.1 扩散模型的基本原理

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

## 一、前向扩散

给定从真实数据分布中采样的数据点，我们定义一个前向扩散过程，在该过程中，我们分步骤向样本添加少量高斯噪声，生成一系列含噪样本。步长由方差调度控制。随着步长的增大，数据样本逐渐失去其可区分的特征。最终，它等效于各向同性的高斯分布。

> [图片内容待重建：img-6b790c6c53f5-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
（*）表示涉及两个高斯分布的合并，数学推导此处暂略。

> [图片内容待重建：img-6b790c6c53f5-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
前向扩散每一步递推的概率表示：

> [图片内容待重建：img-6b790c6c53f5-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
等号右边表示用均值为sqrt(1-β_t)x_t-1，标准差为β_t*I的高斯分布得到x_t.

## 二、反向扩散

1.数学推导

> [图片内容待重建：img-6b790c6c53f5-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
扩散模型的一个巧妙之处在于，当总步数 T足够大时，这个逆向条件概率可以近似为一个高斯分布：

> [图片内容待重建：img-6b790c6c53f5-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
也就是说说，扩散模型先预测一个确定性的噪声值，然后以此得到去噪后x_t-1的概率分布（高斯分布），从分布中抽样得到x_t-1（随机性体现在这里）。下面求解该概率分布。

第一步：应用贝叶斯定理

> [图片内容待重建：img-6b790c6c53f5-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
至此，我们将一个未知的分布分解成了三个已知的高斯分布。

第二步：合并高斯分布

我们希望证明这个分式的值也满足高斯分布，这里可以直接忽略归一化常数，只关注概率密度函数的指数部分，因为高斯分布的指数部分是二次型。从而：

> [图片内容待重建：img-6b790c6c53f5-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里只有y=xt-1为变量。由于f(y)是二次函数，且y=xt-1满足高斯分布，根据高斯分布的公式，这个二次函数的最小值就是高斯分布的均值。

> [图片内容待重建：img-6b790c6c53f5-0013] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0014] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0015] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0016] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
从而最终结果为：

> [图片内容待重建：img-6b790c6c53f5-0017] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
第三步：参数化重写表达式

> [图片内容待重建：img-6b790c6c53f5-0018] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0019] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里的epsilon表示从x_0到x_t的步骤加入的“总等价噪声”，来源于这条式子：

> [图片内容待重建：img-6b790c6c53f5-0020] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
而真实数据是已被确定的情况下，这一步预测的方差仍由αt=1-βt决定：

> [图片内容待重建：img-6b790c6c53f5-0021] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
关于方差的更多讨论见Diffusion Transformer部分。

2.在模型中的应用

以上公式让我们，如果我们能知道在前向过程中第t步所添加的噪声ϵt，那么我们就可以精确地计算出逆向步骤的均值，从而从xt回归到xt−1的分布。但问题在于，我们只知道噪声满足高斯分布N(0,I)，但对于在这个分布中具体采样了什么值，以使得原始的x0变为充满噪声的xt一无所知。

既然我们无法预先知道ϵ，那么就训练一个神经网络ϵθ来学习预测这个噪声。训练目标就是最小化预测噪声ϵθ(xt,t)和真实噪声ϵ之间的均方损失（差距）。这将复杂的分布预测问题转化为更稳定的噪声预测问题，大大降低了模型的学习难度。

这种设置与变分自编码器（VAE）非常相似，因此我们可以使用变分下界来优化负对数似然。

## 三、扩散模型的训练

1.训练时去噪起点的步数随机采样

> [图片内容待重建：img-6b790c6c53f5-0022] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0023] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
2.并行性

扩散模型的每一步都是同时对所有token去噪，因而处理过程是并行的。从概率论的角度，

假设我们要生成一个句子X = {x_1, x_2, x_3, x_4}。

自回归模型模型严格遵循概率的链式法则：

P(X) = P(x_1) P(x_2 | x_1) P(x_3 | x_1, x_2) P(x_4 | x_1, x_2, x_3)

要算x_3，必须先有确定的x_1, x_2。

而扩散模型的目标是直接模拟联合分布P(x_1, x_2, x_3, x_4)。在生成的某一步骤中（例如从X(T+1)去噪到X(T)），模型通常假设：各个位置的预测在当前步骤是条件独立的，也就是x_1(T),x_2(T),...,x_N(T)相互独立。我们只通过上一步的全局上下文x_1(T+1),x_2(T+1),...,x_N(T+1)来推断本步的x_i(T)，即根据“全局的模糊轮廓”来同时填补所有细节。这样，在文本扩散中，公式往往近似为：P(X(T) | X(T+1))=

P(x_1(T)|x_1(T+1),x_2(T+1),...) P(x_2(T)|x_1(T+1),x_2(T+1),...) ...P(x_N(T)|x_1(T+1),x_2(T+1),...)

这样，在一步预测中，猜测x_2就不需要等待x_1先去噪，能提高GPU的利用效率。

在文本生成等任务中，扩散模型的生成质量不如自回归模型。虽然扩散模型可以通过并行一次生成10个词，但由于它在单步生成时，假设了这些词是并行独立生成的（或者仅依赖于上一轮的模糊状态），它往往忽略了词与词之间微小的因果强关联。自回归的做法： “因为第一个词是‘人工’，所以第二个词大概率是‘智能’。”（强因果）纯并行的做法： “我看整体语境像是一个科技话题，所以位置1我猜‘人工’，位置2我猜‘智能’。”如果并行度太高，有时候会出现逻辑不自洽，在更复杂的推理任务中容易导致逻辑断裂。

3.训练算法工作流

> [图片内容待重建：img-6b790c6c53f5-0024] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-6b790c6c53f5-0025] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。

## 参考文献

暂无已核验参考文献。
