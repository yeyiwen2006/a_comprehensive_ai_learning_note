---
title: "39.5 将视觉隐状态融入CoT的推理（论文）"
source_docx: "第5部分 世界模型、多模态生成与具身智能/39.统一架构的多模态理解-生成模型/39.5 将视觉隐状态融入CoT的推理（论文）.docx"
status: "auto-converted"
ocr: "disabled; image content awaits manual reconstruction"
license: "CC BY-NC-SA 4.0"
local_only: false
---

# 39.5 将视觉隐状态融入CoT的推理（论文）

> 本文由本地 Word 原稿自动转换而来。图片内容暂不使用自动 OCR；含公式、图示或表格的图片会在后续人工重建为 Markdown/LaTeX。

> 本文是论文阅读笔记，内容代表对应论文方法或作者理解，不应直接视为领域共识或工程最佳实践。

## 一、文本token-视觉隐状态交替的CoT

论文《Latent Visual Reasoning》《Sketch-in-Latents: Eliciting Unified Reasoning in MLLMs》《HyLaR: Hybrid Latent Reasoning with Decoupled Policy Optimization》针对目前基于文本空间进行多模态推理的瓶颈，让大模型直接在视觉和语言共享的连续隐状态空间内自回归地进行文本token-视觉隐状态（“潜在草图token”）联合生成，二者的维度完全相同（保证输出的隐状态能直接被模型理解）。这种方法大幅提升了模型在细粒度视觉理解任务和需要“空间想象”的推理任务上的表现。模型有两种思考模式：

> [图片内容待重建：img-86910340dbd4-0001] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
其中，处于视觉草图模式（canvas mode）时，对于每一次生成的隐状态不解码为离散token，而是直接加入下一轮生成的上下文：

> [图片内容待重建：img-86910340dbd4-0002] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （一）监督微调

LVR、SkiLa和HyLaR 均以多模态大语言模型作为后训练起点。利用视觉-语言联合推理链数据（如Zebra-CoT 数据集，也为类似于“一段文本推理-一段视觉推理-一段文本推理”形式，不过这里的视觉图像是显式的）对模型进行监督微调。

文本仍使用CE Loss：

> [图片内容待重建：img-86910340dbd4-0003] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
由于视觉隐状态的生成过程是连续的，理论上这样也能监督视觉隐状态推理过程，但效果不佳。我们仍然需要引入视觉损失，以与文本损失加权求和。

LVR和SkiLa直接使用了重建损失的形式。具体而言，即将监督数据集中的图像在经过处理后，利用模型的视觉编码器（冻结以避免“移动靶”问题，让LLM的输出向真实视觉特征对齐）映射为隐状态序列{v_1,v_2,...,v_Tv}。将这组特征序列用标记包裹，在训练时让模型生成的对应隐状态h_t逼近v_t：

> [图片内容待重建：img-86910340dbd4-0004] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
HyLaR则用一个可学习的cross-attention compressor（不用视觉编码器也没有用停止梯度，从而视觉特征会被主动拉进模型的语义空间里，又避免了直接影响模型自身的视觉编码器），把图片压成少量compact embeddings，让h_t逼近下一时刻的embedding即e_t+1，来起到“预测”的作用：

> [图片内容待重建：img-86910340dbd4-0005] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
需要注意的是，一张图并非对应一个Latent。如果有M张中间图像，每张压成N个token，那么大约对应M*N个隐状态。

### （二）强化微调方法一：GRPO_latent

GRPO依靠离散Token的概率分布来计算策略梯度的，但中间的视觉隐状态推理产生的是连续的隐藏状态，没有明确的词汇表概率。

GRPO_latent只对显式的文本输出运用GRPO。另一个问题在于，GRPO在运用经验回放时，需要计算当下策略下过去轨迹对应的概率。而文本生成时用的上下文并不只有前面的文本，还包括前面的隐状态。故在模型的Roll-out（探索采样）阶段，记录模型潜在推理产生的所有隐状态，在计算重要性采样权重时通过回放的方式拼接到计算图中，以恢复上下文环境。

### （三）强化微调方法二：DePO

1.问题背景

HyLaR的作者指出，标准PPO/GRPO/DAPO有两个问题：第一，离散token和连续latent的统计性质不同，文本token的概率分布是分类离散的，轻微扰动很可能并不影响所在类别，而latent向量是连续的，稍微扰动一下，概率值就可能剧烈变化；第二，latent空间的几何不是普通欧氏空间，现代MLLM常用RMSNorm，隐藏状态更像落在高维球面上，方向比长度更重要，用高斯假设去建模会有几何失配。

2.策略概率的表示

因而，DePO把文本位置和latent位置拆开优化，把latent policy建模成von Mises-Fisher（vMF）分布，也就是球面上的自然分布。策略概率表示如下：

> [图片内容待重建：img-86910340dbd4-0006] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0007] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里文本的概率就是我们熟悉的分类词表概率。而对于隐状态的概率，我们知道，PPO/GRPO等算法在实际运行时，往往先进行大量rollout采样，再统一用于策略更新。更新时的新策略和rollout采样时的旧策略是不一样的，但我们手头只有参与过rollout的旧策略动作的reward反馈。因此，我们需要计算新策略和旧策略下这个“旧策略rollout动作”的概率，以通过重要性采样确定在新策略基础上如何更新修正。这条公式的含义也正是：当前新策略的中心方向是u_t，它对旧rollout采样中的“动作”z_t赋予多高的概率密度？

> [图片内容待重建：img-86910340dbd4-0008] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
作者近似认为latent都在单位超球面上，符合vMF分布。vMF分布没有像高斯分布那样用“距离”表征概率，而是选择用“方向（余弦夹角）”，更符合LLM的实际情况。

> [图片内容待重建：img-86910340dbd4-0009] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
重要性比值：

> [图片内容待重建：img-86910340dbd4-0010] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
不难发现，如果两者都是单位向量，r_t必然小于1。普通PPO中ratio可以大于1，因为新策略可能比旧策略更偏好某个token。但在这个设定下，旧策略在自己产生的隐状态处就是最大概率密度，新策略最多只能“和旧策略一样相信这个latent”，不可能超过它。

3.目标函数

在RL训练中，运用了PPO算法：

> [图片内容待重建：img-86910340dbd4-0011] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
这里latent的clip比文本紧，因为连续latent的ratio波动大，如果跟文本用同一个clip，训练会不稳。另外，作者给latent位置用了一个闭式KL正则：

> [图片内容待重建：img-86910340dbd4-0012] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0013] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
注：KL正则项推导

> [图片内容待重建：img-86910340dbd4-0014] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0015] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0016] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
4.任务选择

计算GRPO风格的组内相对优势，如果一个rollout组里全对或全错，标准差接近0，优势值就没有训练意义。所以作者会过滤掉平均准确率太极端的组，只保留有足够奖励方差的样本。

### （四）从隐状态切回文本的策略

1.固定步数：直接指定隐状态推理常量步数后切回文本推理。LVR的论文认为该方案最稳定。

2.在连续空间定义结束向量：在连续特征空间中定义一个向量，标志着隐状态推理的结束。如果在某次得到的隐状态与该向量距离小于一定值，就结束推理。LVR的论文指出该方案的缺点是大模型对高维距离测度表现不稳定，可能一直无法退出。

3.让“闲置”的离散token预测头承担该任务：在隐状态推理中，尽管我们将隐状态而非解码出的离散token用于下一轮推理，但可以让词表预测头不停止工作。在词表中设置特殊的token（<latent_end>），标志着隐状态推理的结束。如果在某次隐状态推理中，解码出的token为<latent_end>，就退出隐状态推理。HyLaR的论文使用该方案。

4.引入模式切换损失：在SFT阶段增加BCE Loss，引导隐状态阶段预测输出接近<lvr_end>。LVR的论文指出，该方案容易导致推理步数坍缩为0。

此外，即使模型没有及时生成结束token，推理系统也不会让它在隐空间中无限循环，而是有一个最大步数上限。

### （五）涌现能力

> [图片内容待重建：img-86910340dbd4-0017] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 二、Monet：用隐变量代替视觉CoT的模型及其训练

### （一）问题背景

> [图片内容待重建：img-86910340dbd4-0018] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （二）整体工作流

> [图片内容待重建：img-86910340dbd4-0019] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （三）训练方法

1.SFT第一阶段：文本CoT预热训练

第一阶段完全不涉及潜变量的生成，仅基于文本和图片进行文本的Next token prediction。这是因为基础模型（Qwen2.5-VL-7B）在预训练时，习惯的是“前文有图、后文作答”的模式，它并没有掌握在思维链（CoT）中途动态“查阅”中间辅助图像（如局部裁剪图、带辅助线的图）的能力。需要给模型喂入大量的图文交错数据（包含显式的真实辅助图像），强制它学会参考这些辅助图像来预测接下来的文本（即“关键观察Token”）。

我们希望第一阶段训练后，预热模型在输出关键观察Token时，其隐藏层特征（Hidden States）能够真正编码并吸收来自辅助图像的视觉信息。只有这样，这些隐藏层特征才能在第二阶段作为高质量的“Teacher”监督信号。

2.SFT第二阶段：允许看辅助图像，学习高质量的潜变量

（1）训练方法：设置一个教师模型（输入包含真实的辅助图像）和一个学生模型（输入潜变量嵌入，但潜变量可以通过注意力机制“看”到辅助图像）。

由于潜变量需要承担辅助图像的作用，学生模型生成的“关键观察文本Token”的隐藏状态，必须与教师模型的对应状态高度一致。除了Next token prediction Loss之外，还定义关键观察token的损失函数：

> [图片内容待重建：img-86910340dbd4-0020] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
为防止模型走捷径（只优化文本而不优化潜变量），强制梯度只能回传更新生成的潜变量。

（2）上下文构建

> [图片内容待重建：img-86910340dbd4-0021] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0022] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0023] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
3.SFT第三阶段：移除辅助图像，训练生成潜变量

训练模型在没有任何辅助图像的情况下生成潜变量。除了Next token prediction之外，还需要将生成的潜变量与第二阶段获得的高质量目标潜变量进行余弦相似度对齐。

> [图片内容待重建：img-86910340dbd4-0024] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
4.RL阶段：VLPO（GRPO在连续空间内的拓展）

> [图片内容待重建：img-86910340dbd4-0025] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
> [图片内容待重建：img-86910340dbd4-0026] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
### （四）训练数据集的构建

> [图片内容待重建：img-86910340dbd4-0027] 原 Word 此处有图片。为避免版权风险，开源版暂不上传图片；自动 OCR 已弃用，后续将依据原稿人工重建为 Markdown/LaTeX。
## 参考文献与引用线索

> 本节由脚本自动检索正文中的引用线索，可能不完整；未能确定来源的位置会在下方标为待补引用。

### 自动检索到的引用线索

- 论文《Latent Visual Reasoning》《Sketch-in-Latents: Eliciting Unified Reasoning in MLLMs》《HyLaR: Hybrid Latent Reasoning with Decoupled Policy Optimization》针对目前基于文本空间进行多模态推理的瓶颈，让大模型直接在视觉和语言共享的连续隐状态空间内自回归地进行文本token-视觉隐状态（“潜在草图token”）联合生成，二者的维度完全相...
- 第一阶段完全不涉及潜变量的生成，仅基于文本和图片进行文本的Next token prediction。这是因为基础模型（Qwen2.5-VL-7B）在预训练时，习惯的是“前文有图、后文作答”的模式，它并没有掌握在思维链（CoT）中途动态“查阅”中间辅助图像（如局部裁剪图、带辅助线的图）的能力。需要给模型喂入大量的图文交错数据（包含显式的真实辅助图像），强制它学会参考这些辅助图像来预测接下来的文本（即“关键观察Token”）。

### 待补引用或版权检查

- [待补引用] 本文含 Word 内嵌图片；开源版未上传图片。若图片来自教材、论文或技术报告，建议人工确认授权、补充来源或重画。
